# Unified Crawler

????????????????????????????????

## ??

- `configs/coin_knowledge.json`?????????????
- `configs/illegal_cases.json`???????????????

?????????????????? `job`?`runtime`?`output` ? `sources`????????? `metadata.knowledge` ? `metadata.illegal_case` ??????????????

## ??

```powershell
python main.py --config configs/coin_knowledge.json
python main.py --config configs/illegal_cases.json
```

?????

```powershell
python main.py --config configs/illegal_cases.json --source-id source_001 --max-pages 1 --max-depth 0
python main.py --config configs/coin_knowledge.json --dry-run
```

?????`--source-id`?`--max-pages`?`--max-depth`?`--output-dir`?`--dry-run`?`--no-progress`?

## ??

???????? `output.directory`????

- `records.jsonl`
- `failures.jsonl`
- `crawl_report.json`

`data/` ?? `.gitignore` ???????????
